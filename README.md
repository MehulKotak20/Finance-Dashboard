# Finance Dashboard (React + Vite)

Minimal React + Vite scaffold.

Getting started (Windows PowerShell):

```powershell
npm install
npm run dev
```

Open the URL shown in the terminal (usually http://localhost:5173).
